<% content_for :title, "Editing #{@user.andrew_id}" %>
<div class="container" id="cEvent">
	<div class="event-form">
		<%= render 'form' %>
	</div>
	<%= link_to 'Show', @user %> |
	<%= link_to 'Back', users_path %>
</div>
