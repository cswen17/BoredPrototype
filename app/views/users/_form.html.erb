<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<fieldset class="form-info">

  		<div class="field">
    		<%= f.label :first_name %>
    		<%= f.text_field :first_name %>
  		</div>
  		
		<div class="field">
    		<%= f.label :last_name %>
    		<%= f.text_field :last_name %>
  		</div>
  
		<div class="field">
    		<%= f.label :andrew_id %>
    		<%= f.text_field :andrew_id %>
  		</div>

        <div class="field">
            <%= f.label :is_admin %>
            <%= f.check_box :is_admin %>
        </div>

        <div class="field">
            <%= f.label :is_org_leader %>
            <%= f.check_box :is_org_leader %>
        </div>

        <div class="field">
            <%= f.label :is_developer %>
            <%= f.check_box :is_developer %>
        </div>

</fieldset>
  <div class="actions">
    <%= f.submit class: "paper-raised-button" %>
  </div>
<% end %>
